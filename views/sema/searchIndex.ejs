<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Confirmação da Inscrição</title>
    <link rel="icon" href="/images/favoicon.png" type="image/x-icon" />
    <!-- Incluindo Tailwind CSS via CDN -->
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        .gradient-bg {
            background: linear-gradient(135deg, #1e3a8a, #3b82f6);
        }
        .input-focus {
            transition: all 0.3s ease-in-out;
        }
        .input-focus:focus {
            border-color: #3b82f6;
            box-shadow: 0 0 8px rgba(59, 130, 246, 0.5);
        }
    </style>
</head>
<body class="min-h-screen flex items-center justify-center gradient-bg">
    <div class="bg-white shadow-xl rounded-xl p-8 max-w-lg w-full mx-4">
        <h1 class="text-2xl font-bold text-center text-gray-800 mb-6">CONFIRME SUA INSCRIÇÃO</h1>
        
        <!-- Formulário com inputs para CPF e RG -->
        <form action="/search" method="POST" class="space-y-6">
            <div>
                <label for="cpf" class="block text-sm font-medium text-gray-700 mb-1">CPF</label>
                <input 
                    type="text" 
                    id="cpf" 
                    name="cpf" 
                    placeholder="Digite o CPF (com ou sem pontuação)" 
                    class="w-full px-4 py-2 border border-gray-300 rounded-lg input-focus"
                    required
                >
            </div>
            
            <div>
                <label for="rg" class="block text-sm font-medium text-gray-700 mb-1">RG</label>
                <input 
                    type="text" 
                    id="rg" 
                    name="rg" 
                    placeholder="Digite o RG" 
                    class="w-full px-4 py-2 border border-gray-300 rounded-lg input-focus"
                >
            </div>

            <!-- Botão de envio -->
            <div class="text-center">
                <button 
                    type="submit" 
                    class="bg-blue-600 text-white font-semibold py-3 px-6 rounded-lg hover:bg-blue-700 transition duration-300"
                >
                    Buscar
                </button>
            </div>
        </form>
    </div>
</body>
</html>